@model IEnumerable<MagisterFluentAPI.Models.Professores>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MatriculaProfessor)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NomeProfessor)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MatriculaProfessor)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NomeProfessor)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdProfessor }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdProfessor }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdProfessor })
        </td>
    </tr>
}

</table>
